* Add a input field updates a property ("username") via Two-Way-Binding.
* Output the ussername property via String Interpolation (in a paragraph below the input).
* Add a button which may only be clicked if the username is NOT an empty String.
* Upon clicking the button, the username should be reset to an empty String.